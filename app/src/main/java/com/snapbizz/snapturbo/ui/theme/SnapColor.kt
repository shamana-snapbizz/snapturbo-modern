package com.snapbizz.snapturbo.ui.theme

import androidx.compose.ui.graphics.Color

object SnapColor {
        val DarkTransparent = Color(0xD9000000)
        val Transparent = Color(0x00000000)

        // Keypadw
        val textColor = Color(0xFFFFFFFF)
        val KeypadNumberText = Color(0xFFFFFFFF)
        val HorzKeypadNumberText = Color(0xFF999999)
        val KeypadPressedText = Color(0xFFFFFFFF)
        val KeypadUnselectedText = Color(0xFF666666)
        val KeypadSelectedText = Color(0xFF50A6FF)
        val KeypadDivider = Color(0xFF39A0FF)

        // Text
        val SelectedText = Color(0xFF39A0FF)
        val UnselectedText = Color(0xFF484747)

        // Borders / Dividers
        val Divider = Color(0xFFE3E5E5)
        val ProductBorder = Color(0x80CCCCCC)
        val ProductSelectedBorder = Color(0x80C0DEFE)
        val CategoryBorder = Color(0xFFDBDBDF)
        val BillBorder = Color(0xFFDBDBDF)

        // Backgrounds
        val DefaultBackground = Color(0xFFEAEFF1)
        val ProductBackgroundDark = Color(0xFFF9F9F9)

        // Product / Inventory
        val ProductText = Color(0xFF615F5F)
        val InventoryMenuText = Color(0xFFFFFFFF)

        // Customer / Distributor
        val CustomerListHeader = Color(0xFF5A98D1)
        val DistributorEditText = Color(0xFF000000)
        val DistributorHintText = Color(0xFF696969)

        // Toasts
        val ToastBackground = Color(0xFF35373C)
        val ToastErrorBorder = Color(0xFFE25353)
        val ToastInfoBorder = Color(0xFF4E91CF)
        val ToastSuccessBorder = Color(0xFF19CF51)
        val ToastWarningBorder = Color(0xFFFDC468)

        // Offers / Bills
        val StoreNameOffer = Color(0xFF3A94E2)
        val Bill1Hint = Color(0xFF3D8D57)
        val Bill2Hint = Color(0xFFFFC000)
        val Bill3Hint = Color(0xFFFF0000)
        val Bill4Hint = Color(0xFF60A80B)

        // Sync
        val SyncSummaryBackground = Color(0xFF0388D5)
        val primaryColor = Color(0xFF006DA8)
}